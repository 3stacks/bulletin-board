{
  "name": "bulletin-board",
  "version": "0.2.0",
  "description": "A local bulletin board so concurrent agents can claim working directories and resources (docker containers, ports, deploy slots) and announce intent. A toolgate sidecar.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "bb": "./src/cli.ts"
  },
  "exports": {
    ".": "./src/board.ts",
    "./toolgate": "./src/toolgate-policy.ts"
  },
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "keywords": ["claude-code", "toolgate", "agents", "coordination", "lock", "bulletin-board"]
}
